---
title: 'Pilot sturgeon alert system'
format: dashboard
---

```{r}
library(data.table)
library(ggplot2)

parsed_db  <- fread('dashboard/db_parsed.csv')


ggplot(parsed_db) +
    geom_line(aes(x = cpu_time, y = cpu_temp))
```